# Render Blueprint：连接 Git 仓库后可用「Apply」一键创建 Web Service
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: flyvio
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
